[package]
name = "mosys"
version = "2.0.0"
authors = ["The Chromium OS Authors"]
build = "build.rs"
links = "mosys"

[profile.release]
lto = true
panic = 'abort'

[dependencies]
io_jail = { path = "io_jail" }
